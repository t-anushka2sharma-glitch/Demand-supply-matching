# Java Exercise 1 - Part 3/4 [Spring]
# Web BE application settings.

server.port=8080
spring.application.name=demand-supply-matching

# Note: DB credentials for the JDBC persistence layer (OrderDao/TradeDao,
# reused from Part 2) live separately in db.properties, not here.
