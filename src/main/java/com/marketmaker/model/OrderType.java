package com.marketmaker.model;

/**
 * Distinguishes a supply order (published by a farmer/trader offering produce)
 * from a demand order (published by a customer/trader requesting produce).
 */
public enum OrderType {
    SUPPLY,
    DEMAND
}
